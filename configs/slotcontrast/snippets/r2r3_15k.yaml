# R2+R3 override (T1d). target_encoder + delayed unfreeze; no R1.
# Inherits optimizer.lr from base config.
optimizer:
  param_groups: null
  lr_scheduler:
    name: exp_decay_with_warmup
    warmup_steps: 2500
    decay_steps: 15000
