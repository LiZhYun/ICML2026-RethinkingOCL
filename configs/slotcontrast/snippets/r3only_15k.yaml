# R3-only override (T1d). Delayed unfreeze kept; no R1, no R2.
# Inherits optimizer.lr from base config.
optimizer:
  param_groups: null
  lr_scheduler:
    name: exp_decay_with_warmup
    warmup_steps: 2500
    decay_steps: 15000
model:
  target_encoder: null
  target_encoder_input: null
